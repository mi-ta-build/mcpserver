Sample implementation of an SSE-based MCP server.
This application runs on OpenLiberty.

To run it, create a clone on an environment running the JDK and then execute the following command  
mvnw liberty:run
